import { SpinnerWrapper, StyledSpinner } from "./LoadingSpinner.styles";

const LoadingSpinner = () => {
  return (
    <SpinnerWrapper>
      <StyledSpinner />
    </SpinnerWrapper>
  );
};

export default LoadingSpinner;
